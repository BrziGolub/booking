<Window x:Class="Booking.View.ShowTourImages"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Booking.View"
        mc:Ignorable="d"
        Title="ShowTourImages" Height="450" Width="450"
		WindowStartupLocation="CenterScreen"
		ResizeMode="NoResize">
    <Grid Margin="16">

		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>

		<Label Grid.Row="0" Content="Images" FontSize="24" Margin="0 0 0 8"/>

		<ListBox x:Name="ListBoxImages" Grid.Row="1" Margin="0 8">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Image Source="{Binding Url}" Width="350"/>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<Button Grid.Row="2" Content="Close(F5)" Margin="0 8 0 0" Width="100" Height="27" Click="ButtonClose" HorizontalAlignment="Right"/>
		
	</Grid>
</Window>
